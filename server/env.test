# Test Environment Variables für Projektseite v3.0

# Test Environment
NODE_ENV=test

# Test Database Configuration
TEST_DATABASE_URL="postgresql://dev:dev_password@localhost:5433/projektseite_dev?schema=public"
DATABASE_URL="postgresql://dev:dev_password@localhost:5433/projektseite_dev?schema=public"

# Test JWT Configuration
JWT_SECRET=test-super-secret-jwt-key-for-testing-only
JWT_EXPIRES_IN=24h

# Test Server Configuration
PORT=3002
API_URL=http://localhost:3002/api

# Test Frontend Configuration
FRONTEND_URL=http://localhost:3001

# Test Security Configuration
BCRYPT_ROUNDS=4

# Test Logging Configuration
LOG_LEVEL=error

# Test Rate Limiting (more permissive for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# Test File Upload Configuration
MAX_FILE_SIZE=1048576
UPLOAD_PATH=./test-uploads

# Test Email Configuration (disabled)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Test Monitoring Configuration (disabled)
SENTRY_DSN=
PROMETHEUS_ENABLED=false

# Test Feature Flags
ENABLE_SMTP_CHECK=false
ENABLE_STORAGE_CHECK=false
ENABLE_CACHE_CHECK=false

# Test Database Pool Configuration
DATABASE_POOL_MIN=1
DATABASE_POOL_MAX=5
DATABASE_POOL_IDLE_TIMEOUT=10000
DATABASE_POOL_ACQUIRE_TIMEOUT=10000
